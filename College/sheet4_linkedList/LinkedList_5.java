package com.company.Linked_List;

public class LinkedList_5 {
    //1- take the input from the user using Scanner && create a linkedList

    //2- save every char from zero to the end of the input string in a linkedList of chars using (insert)

    // insert every new char in the head of the list so the last char inserted is the first to come out using delete

    //3- using delete on the linkedList take every char and add it to the output

    /*
        output = "";
        output += list.delete();
     */

    /*
        ->the list class is the same as the first problem

        ->and link is the same

        -> all that need to be written is the testClass

     */

}
